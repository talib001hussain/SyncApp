name=postgres-sink-connector
connector.class=io.confluent.connect.jdbc.JdbcSinkConnector
tasks.max=1
topics=C10Partner
connection.url=jdbc:postgresql://localhost:5432/your_database_name
connection.user=your_postgres_username
connection.password=your_postgres_password
auto.create=true           # Creates the table if it does not exist
auto.evolve=true           # Alters table schema as needed
insert.mode=upsert         # Use `insert` for inserts only
pk.mode=record_value       # Use `record_value` if records contain primary keys
pk.fields=partner_id       # Specify primary key field(s) (from your data)
